spring:
  datasource:
    jdbcUrl: jdbc:postgresql://localhost:5432/pdv
    username: postgres
    password: 12345
    driverClassName: org.postgresql.Driver
  jackson:
    time-zone: America/Argentina/Buenos_Aires
  jpa:
    generate-dll: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: create
    show-sql: false

#tenants
tenants:
  datasources:
    pdv:
      jdbcUrl: jdbc:postgresql://localhost:5432/pdv
      driverClassName: org.postgresql.Driver
      username: postgres
      password: 12345
    servipack:
      jdbcUrl: jdbc:postgresql://localhost:5432/servipack
      driverClassName: org.postgresql.Driver
      username: postgres
      password: 12345
    sprosario:
      jdbcUrl: jdbc:postgresql://localhost:5432/sprosario
      driverClassName: org.postgresql.Driver
      username: postgres
      password: 12345
    test:
      jdbcUrl: jdbc:postgresql://localhost:5432/test
      driverClassName: org.postgresql.Driver
      username: postgres
      password: 12345

server:
  port: 8000